const NumberOfEvents = () => {

    return (
        <div id="event-number">
            <input type="text" placeholder="Enter number of events" />
        </div>
    );
}

export default NumberOfEvents;